<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:Farm.Model"
             x:Class="Farm.Views.Livestock"
             Title="Livestock">

    <Grid RowDefinitions="*,50,20" RowSpacing="20">
        <Border StrokeShape="RoundRectangle 10" Margin="40" Padding="10">
            <CollectionView ItemsSource="{Binding animals}" ItemsUpdatingScrollMode="KeepItemsInView" SelectionMode="Single">
                <CollectionView.Header>
                    <Grid BackgroundColor="Black" ColumnDefinitions="*,*,*,*,*,*" Padding="5">
                        <Label Text="Type" Grid.Column="0"/>
                        <Label Text="ID" Grid.Column="1"/>
                        <Label Text="Weight" Grid.Column="2"/>
                        <Label Text="Colour" Grid.Column="3"/>
                        <Label Text="Expense" Grid.Column="4"/>
                        <Label Text="Milk/Wool" Grid.Column="5"/>
                    </Grid>
                </CollectionView.Header>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="model:Animal">
                        <Grid ColumnDefinitions="*,*,*,*,*,*">
                            <Label Text="{Binding AnimalType}" Grid.Column="0"/>
                            <Label Text="{Binding ID}" Grid.Column="1"/>
                            <Label Text="{Binding Weight}" Grid.Column="2"/>
                            <Label Text="{Binding Colour}" Grid.Column="3"/>
                            <Label Text="{Binding Expense}" Grid.Column="4"/>
                            <Label Text="{Binding Milk}" Grid.Column="5" 
                            IsVisible="{Binding AnimalType, Converter={StaticResource CowVisibilityConverter}}"/>
                            <Label Text="{Binding Wool}" Grid.Column="5" 
                            IsVisible="{Binding AnimalType, Converter={StaticResource SheepVisibilityConverter}}"/>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Border>
        <HorizontalStackLayout Grid.Row="1" HorizontalOptions="Center" VerticalOptions="Center">
            <Button Text="Add New"/>
        </HorizontalStackLayout>

    </Grid>
</ContentPage>